---
image:
  tag: 0.33.7-1852a794-amd64

imagePullSecrets:
  - name: ghcr-creds

database:
  host: postgresql.postgres.svc.cluster.local
  dbName: test-directory
  sslMode: disable
  admin:
    credentialsSecret: pg-credentials
  reader:
    credentialsSecret: pg-directory-reader-credentials

cache:
  sizeMB: 100

sshAdminKeys:
  configMap:
    name: directory-admin-keys
    key: authorized_keys
